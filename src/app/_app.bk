import type { AppProps } from "next/app";
import Layout from "../components/Layout.bk";
import "../app/globals.css";
import { AuthProvider } from "../context/auth";

export default function App({ Component }: AppProps) {
  return (
    <AuthProvider>
      <Layout>
        {/* pages配下にpropsを渡す */}
        {/* <Component {...pageProps} />; */}
        <Component />;
      </Layout>
    </AuthProvider>
  );
}
